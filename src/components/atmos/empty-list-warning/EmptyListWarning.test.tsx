import { render, screen } from "@testing-library/react";
import EmptyListWarning from "./EmptyListWarning";

describe("Empty list warning component", () => {
  test("Should render correctly", () => {
  });
});
